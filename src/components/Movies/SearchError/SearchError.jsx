import React from "react";
import "./SearchError.css";

function SearchError() {
    return (
        <p className="search-error_not-found">
            Ничего не найдено
        </p>
    );
}

export default SearchError;